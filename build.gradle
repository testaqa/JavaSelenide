plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

dependencies {

}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

tasks.withType(Test) {
    systemProperties += System.properties
}